<h1>商品詳細</h1>
 <div><%= image_tag item.image, size: "100x100"%></div>
 <div>商品名</div>
  <%= @item.name %>
 <div>商品説明</div>
  <%= @item.introduction %>
 <div>ジャンル</div>
  <%= %>
 <div>税込価格(税抜価格)</div>
  <p><%= @item.price %>円</p>
 <div>販売ステータス</div>
  <%= @item.is_available %>

 <div><%= link_to "編集する", edit_admin_item_path, class: "btn btn-primary" %></div>
